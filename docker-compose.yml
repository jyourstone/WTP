services:
  cissi-traning:
    build: .
    container_name: cissi-traning
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DB_PATH=/app/data/traningskalender.db
